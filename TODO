 * Get LDAP auth working
 * Check stella info with PyMoira before sending mail for *.mit.edu
   * Package PyMoira into an rpm and get installed on scripts servers
     * Package libmoira into an rpm and get installed on scripts servers
 * Deal with the new vhost reification system.
